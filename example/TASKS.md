# Project Tasks

1.  **Project tasks** [NOT DONE]
    - [ ] Generate a random integer between 1 and 100.
    - [ ] Convert the user's input to compare with the generated number.
    - [ ] Print "Too High" if the guess is larger than the target.
    - [ ] Print "Too Low" if the guess is smaller than the target.
    - [ ] Print a success message and break the loop if the guess is correct.
    - [ ] Handle non-numeric inputs without crashing.
    - [ ] Track and display the number of attempts it took to win.
    - [ ] allow the user to play again (prompt for "Play again? y/n").

2.  **CHANGE output COLOR** [NOT DONE]
    - [ ] Show the results in bright red color

3.  **Web interface** [NOT DONE]
    - [ ] Generate a web interface for the game
    - [ ] create a web server that serves the page
